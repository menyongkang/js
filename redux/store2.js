export const createStore = () => {
  // 公共状态
  let currentState = {};

  // 获取getter
  function getState() {}
  // 设置stter
  function dispatch() {}
  //  广播subscribe
  function subscribe() {}

  return{getState,dispatch,subscribe}
}